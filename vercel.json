{
  "buildCommand": "npx convex deploy --cmd 'npm run build' && if [ \"$VERCEL_ENV\" = \"preview\" ]; then npx convex import --preview-name \"$VERCEL_GIT_COMMIT_REF\" --replace-all -y seed_data.zip; fi"
}
